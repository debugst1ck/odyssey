[project]
name = "odyssey"
version = "0.1.0"
description = "A model training framework inspired by Pytorch Lightning"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["torch"]

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
